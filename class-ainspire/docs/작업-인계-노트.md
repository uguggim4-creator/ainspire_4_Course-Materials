# 작업 인계 노트 — class-ainspire 포털 (2026-05-26 기준)

다른 세션에서 이어서 작업할 때 이 문서부터 읽으세요. 표준은 `교안제작-매뉴얼.md`.

---

## 0. 한 줄 요약
AInspire 4기 자료 포털(`class-ainspire/`)에 바이브코딩 교안을 만들고, 포털 카드·드라이브 구조를 정리하는 중. **사전(glossary) 확장 1건이 미완료** — 아래 4번 참고.

---

## 1. 지금 포털에 있는 것 (data-materials.js, 카드 6개)

| id | 제목 | 타입 | 가리키는 곳 |
|---|---|---|---|
| media-tools | AI 미디어 도구 모음 | link | materials/미디어도구/media-tools-deck.html |
| vibe-journey | 바이브코딩 여정 — AI 영상 웹앱 만들기 | link | materials/바이브코딩용어/vibe-journey-deck.html |
| week2-folder | 2기 자료 모음 (Google Drive) | folder | (드라이브 폴더, 아래 3번) |
| cineguide-main / -glossary / -resources | CineGuide ×3 | link | cineguide/*.html |

- **삭제됨**: `vibe-terms`(바이브코딩 용어 지도, 사전형 슬라이드 29장) — 카드·HTML 파일 모두 삭제 완료.
- `fileType: "folder"`는 이번에 새로 추가한 타입. index.html의 openMaterial()에서 `embeddedfolderview`로 모달 미리보기 + 새 탭은 폴더 열기로 처리.

---

## 2. 바이브코딩용어 폴더 현황 (materials/바이브코딩용어/)
- `vibe-journey-deck.html` — **완성**. "AI 영상 웹앱 만들기" 시나리오 deck 19장.
  - 각 STEP 좌측 = 나↔AI 채팅 대화(실제 에이전트 톤), 우측 = 그 대화에 나온 용어 카드(등장 순서대로 정렬).
  - 우측 카드 많은 슬라이드(STEP1·2·3구조)는 `.tool-right--tall`로 제목 라인까지 끌어올림.
  - 골격: 고정 1280x720 + scaleDeck, `?embed=1` 임베드, `?debug=1` 주석모드, const TOTAL=19.
- `vibe-glossary.html` — **진짜 사전(아코디언) 테스트본**. 현재 **23개 용어 / 6그룹**.
  - `<details>` 아코디언(클릭하면 그 자리 펼침) + 상단 검색창 + 하단 실전요청예시.
  - 아직 **포털 카드 미등록**(테스트 중).

원본 자료집:
- `바이브코딩_풀버전_용어_자료집.md` — 9그룹 **90개 용어**(`###` 기준).
- `바이브코딩_핵심_용어_자료집.md` — 6그룹 핵심본(현재 glossary가 이 수준, 23개).

---

## 3. 구글 드라이브 구조 (공개 폴더 "AInspire 4기 수강생 자료 (공개)")
- 상위 공개 폴더 ID: `1XD7fvn9UoLc6WyKxlZBZvXjP6k519cwr`
- 그 안에 이번에 **하위 폴더 2개 생성**:
  - `2기 강의` = `14OqhlmSCvP3ccTSdQaFE3EWOdOiDUH8y`
  - `3기 강의`(빈 폴더) = `1NHRPTZikYFGbjVeUZnwaomsW79NLhE9u`
- **할 일(사용자가 직접)**: 상위 폴더에 평평하게 있는 PDF/XLSX 16개를 `2기 강의` 폴더로 드래그 이동. (드라이브 MCP에 이동/삭제 도구 없음 — 복사만 가능해서 직접 옮기기로 함. 드래그 이동은 driveId 안 바뀌므로 코드 수정 불필요.)
- **미결정**: week2-folder 카드가 상위 폴더(현재)를 가리킬지, `2기 강의` 하위 폴더(`14Oqhl...`)를 가리킬지. 파일 이동 후 결정.
- 주의: 새 하위 폴더 2개의 공개 권한 상속 여부는 브라우저에서 확인 필요(MCP로 권한 변경 불가).

---

## 4. ★ 미완료 / 다음에 할 일
1. **사전(glossary) 90개 확장 — 미반영**. `vibe-glossary.html`을 풀버전 자료집(90개·9그룹)으로 확장하려 했으나 파일은 여전히 23개/6그룹. 재작업 필요.
   - 방식: 기존 아코디언 `.term` 마크업 그대로 복제, 자료집 정의 충실히 옮김, 그룹 6→9개 재편.
   - 추가 대상 67개 목록은 `바이브코딩_풀버전_용어_자료집.md`에서 현재 23개 빼면 됨.
   - 절대 보존: head CSS/토큰/Paperlogy, 검색 #search 스크립트, 하단 .practice, debug-annotate.js 로드.
2. 사전 완성 후 **포털 카드 등록** 여부 결정(data-materials.js).
3. 드라이브 16개 파일 이동(사용자) + week2-folder 카드 타깃 확정.

---

## 5. 작업 원칙 (이번 세션에서 학습한 것)
- **잘 되던 기능은 기준선으로 보존, 최소 변경**. debug-annotate.js를 과하게 건드려 여러 번 깨뜨린 적 있음([[debug-annotate-minimal-change]]).
- deck 골격(scaleDeck/embed/goTo/debug 로드/const TOTAL) 절대 변경 금지.
- 디자인 토큰 색만 사용(새 hex 금지 — `#e8d8ff` 도입 사고 있었음).
- 존댓말, 이모지 금지, 일상 비유 남발 금지(매뉴얼 2장).
- 로컬 확인: `cd class-ainspire && python -m http.server 8765` → http://127.0.0.1:8765/

## 6. 로컬 서버
이 세션에서 8765 포트로 띄워둠(백그라운드). 다른 세션이면 다시 띄워야 함.
